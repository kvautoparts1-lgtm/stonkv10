# --- Filters ---
# 24h volume must fall within this USD range
VOLUME_MIN_USD=5000
VOLUME_MAX_USD=10000

# Minimum pending ("to be distributed") rewards, in USD
REWARD_MIN_USD=46

# Maximum number of on-chain holders
MAX_HOLDERS=3

# --- Run behavior ---
# 'loop' = run forever, rescanning every POLL_INTERVAL_MS (good for Railway)
# 'once' = run a single pass then exit (good for Railway Cron)
RUN_MODE=loop
POLL_INTERVAL_MS=900000

# Delay between StonkFun API calls, in ms (keeps you under the 300/min limit)
REQUEST_DELAY_MS=250

# How long to wait for a single StonkFun API response before giving up and
# retrying, in ms. Raise this if you see repeated timeout/abort warnings on
# deep-pagination pages.
API_TIMEOUT_MS=20000

# Delay between Solana RPC calls, in ms
RPC_REQUEST_DELAY_MS=500

# Your own Solana RPC endpoint (recommended — the public one is heavily
# rate-limited and shared). Leave unset to use the public endpoint.
# SOLANA_RPC_URL=https://your-rpc-provider.example.com

# Print raw JSON for the first few tokens/rewards responses, to verify field names
DEBUG=0

# Force exact dot-paths if auto-detection ever picks the wrong field, e.g.
# REWARD_USD_PATH=rewards.pending.usd
# VOLUME_USD_PATH=volume.usd

# --- Optional Telegram alerts ---
# 1. Create a bot with @BotFather, get its token.
# 2. Add the bot to your channel as admin.
# 3. Chat ID can be the channel's @username (e.g. @mychannel) or its numeric id.
# TELEGRAM_BOT_TOKEN=
# TELEGRAM_CHAT_ID=
